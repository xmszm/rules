---
name: xmszm-setup-guide
description: xmszm Skill 快速安装和使用指南。
---

# xmszm Skill 快速开始指南

> 本指南帮助你快速安装和开始使用 xmszm Skill

## 📋 前置要求

- Claude Code（最新版本）
- 访问 `~/.claude` 目录的权限
- Git 或任何文本编辑器

## 🚀 快速安装（3 分钟）

### 步骤 1：查看文件结构

确认 xmszm 目录包含以下内容：

```
xmszm/
├── SKILL.md              ✓ 必需
├── guides/
│   ├── CODE-WORKFLOW.md
│   └── LOG-SKILL.md
├── rules/
│   ├── archive-format.md
│   ├── log-operations.md
│   └── ...
├── scripts/
│   ├── sync-skill.sh     # 同步脚本
│   ├── sync-skill.bat
│   └── README.md         # 详细文档
└── templates/
    └── *.md.template
```

### 步骤 2：运行同步脚本

**Linux/Mac/Git Bash:**

```bash
cd xmszm/scripts
chmod +x sync-skill.sh
./sync-skill.sh
```

**Windows (PowerShell/CMD):**

```cmd
cd xmszm\scripts
sync-skill.bat
```

### 步骤 3：验证安装

检查 Claude Code 是否识别到 skill：

```bash
ls ~/.claude/skills/xmszm/
# 应该能看到 SKILL.md 和其他文件
```

✅ 完成！现在可以在 Claude Code 中使用 xmszm skill 了。

---

## 💡 使用方法

### 场景 1：代码开发任务

```
用户: "修改登录页的样式"
↓
SKILL.md 识别为代码开发
↓
读取 guides/CODE-WORKFLOW.md
↓
执行完整工作流（定→查→搜→问→做→记）
```

### 场景 2：快速记录变更

```
用户: "记录一下刚修改的代码"
↓
SKILL.md 识别为日志操作
↓
读取 guides/LOG-SKILL.md
↓
快速记录变更，无需完整流程
```

---

## 🔄 更新 Skill

当修改 xmszm 文件后，需要重新同步到 `.claude/skills`：

### 快速更新

```bash
cd xmszm/scripts
./sync-skill.sh -f        # -f 表示强制覆盖，无需确认
```

### 选项说明

| 选项 | 说明 |
|------|------|
| `-f` | 强制覆盖，不需确认 |
| `--no-backup` | 不创建备份 |
| `--dry-run` | 模拟运行，查看操作但不执行 |

详见 [scripts/README.md](../scripts/README.md)

---

## 📚 文件导航

### 核心文件

| 文件 | 用途 |
|------|------|
| `SKILL.md` | **入口**，任务路由判断 |
| `guides/CODE-WORKFLOW.md` | 代码开发详细工作流 |
| `guides/LOG-SKILL.md` | 日志操作详细工作流 |

### 规则文件

| 文件 | 用途 | 何时需要 |
|------|------|---------|
| `rules/project-detection.md` | 项目定位规则 | Step 0 |
| `rules/task-type-mapping.md` | 任务类型映射 | Step 1 |
| `rules/archive-format.md` | 归档格式规范 | Step 5 |
| `rules/log-operations.md` | 日志操作规则 | 日志任务 |
| `rules/search-strategy.md` | 代码搜索策略 | Step 2 |
| `rules/edge-cases.md` | 特殊场景处理 | 遇到问题时 |

### 辅助工具

| 脚本 | 用途 |
|------|------|
| `scripts/sync-skill.sh` | 同步 skill 到 ~/.claude/skills（Linux/Mac/Git Bash） |
| `scripts/sync-skill.bat` | 同步 skill 到 ~/.claude/skills（Windows） |
| `scripts/log-change.sh` | 快速命令行记录日志 |
| `scripts/migrate-changelog.sh` | 迁移旧格式 changelog 到新格式 |

---

## ⚙️ 初始化项目归档（如需要）

如果项目中还没有 `.xmszm` 归档目录，xmszm 会自动初始化：

**自动触发**：在代码开发任务的 Step 0 时，如果检测到 `.xmszm` 不存在，会自动创建。

**手动初始化**：

```bash
# 在项目根目录运行
mkdir -p .xmszm/{pages,guides,changelog}
cat > .xmszm/index.md << 'EOF'
# 项目归档索引

## 快速导航
- [页面](./pages-index.md)
- [API接口](./guides/)

EOF
```

---

## 🆘 常见问题

### Q：如何判断 skill 是否正确安装？

A：检查 `~/.claude/skills/xmszm/SKILL.md` 是否存在：

```bash
# Linux/Mac
test -f ~/.claude/skills/xmszm/SKILL.md && echo "✓ 已安装" || echo "✗ 未安装"

# Windows PowerShell
if (Test-Path "$env:USERPROFILE\.claude\skills\xmszm\SKILL.md") {
    Write-Host "✓ 已安装"
} else {
    Write-Host "✗ 未安装"
}
```

### Q：更新后 Claude Code 没有识别新版本？

A：
1. 重启 Claude Code
2. 使用 `--dry-run` 检查是否同步成功
3. 手动检查文件是否存在：`ls -la ~/.claude/skills/xmszm/`

### Q：能否在不同项目中使用 xmszm？

A：可以。xmszm 是全局 skill，安装在 `~/.claude/skills/` 中，所有项目都可以使用。每个项目可以有自己独立的 `.xmszm` 归档目录。

### Q：如何恢复之前的版本？

A：备份文件保存在 `~/.claude/skills/` 目录下：

```bash
# 查看备份
ls -la ~/.claude/skills/xmszm.backup-*

# 恢复备份
rm -rf ~/.claude/skills/xmszm
cp -r ~/.claude/skills/xmszm.backup-20260107-143000 ~/.claude/skills/xmszm
```

### Q：可以自定义规则吗？

A：可以。修改 `rules/` 目录下的文件，然后运行 `./sync-skill.sh -f` 更新。

---

## 📖 深入学习

### 基础概念

- **Project Root (`$PROJECT_ROOT`)**：项目的根目录
- **Archive Path (`$ARCHIVE_PATH`)**：通常为 `$PROJECT_ROOT/.xmszm`
- **Skill**：Claude Code 中的命令工具，SKILL.md 是入口文件

### 工作流程

**代码开发流程**：
```
定（项目定位）→ 查（归档定位）→ 搜（兜底补充）→ 问（确认细节）→ 做（实施）→ 记（归档）
```

详见 [guides/CODE-WORKFLOW.md](../guides/CODE-WORKFLOW.md)

**日志操作流程**：
```
识别操作类型 → 快速记录/历史查询/统计分析
```

详见 [guides/LOG-SKILL.md](../guides/LOG-SKILL.md)

### 高级特性

- **多项目环境**：自动检测和管理多个项目
- **格式自动检测**：支持新/旧两种 changelog 格式
- **智能路由**：根据任务类型自动选择执行流程
- **边界情况处理**：详见 [rules/edge-cases.md](../rules/edge-cases.md)

---

## 🔧 故障排除

### 脚本不可执行（Linux/Mac）

```bash
chmod +x sync-skill.sh
```

### 找不到 ~/.claude 目录

```bash
# 创建目录
mkdir -p ~/.claude/skills
```

### 权限被拒绝

```bash
# 检查权限
ls -ld ~/.claude/skills/

# 修复权限
chmod 755 ~/.claude/skills/
```

---

## 📞 获取帮助

- **路由不确定？**：查看 SKILL.md 的 [任务类型智能路由](../SKILL.md#-任务类型智能路由) 部分
- **工作流细节？**：读取 guides/ 下对应的文档
- **具体规则？**：查看 rules/ 目录下的文件
- **脚本问题？**：阅读 [scripts/README.md](../scripts/README.md)

---

## 下一步

✅ 已安装 xmszm？

现在可以：

1. **创建项目归档**（可选）
   ```bash
   mkdir -p .xmszm/pages
   mkdir -p .xmszm/guides
   mkdir -p .xmszm/changelog
   ```

2. **开始使用**
   - 在 Claude Code 中描述代码任务
   - xmszm 会自动识别任务类型并执行相应流程

3. **管理变更**
   - 快速记录：用户说"记录..."时自动触发
   - 或运行命令：`./scripts/log-change.sh -m "修复bug"`

4. **查看归档**
   - 代码变更自动记录到 `.xmszm/changelog/`
   - 可以查看 `.xmszm/pages/` 了解各页面的结构

---

## 许可和支持

xmszm Skill 是一个开源项目。遇到问题时：
- 检查 rules/edge-cases.md
- 查看相关文档的示例
- 使用 `--dry-run` 测试操作

祝你使用愉快！🎉
