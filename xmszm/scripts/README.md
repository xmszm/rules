# xmszm Skill 更新脚本

## 概述

两个脚本用于将当前的 `xmszm` 目录更新到 Claude Code 的 skills 目录：
- `sync-skill.sh` - Linux/Mac/Git Bash 版本
- `sync-skill.bat` - Windows 原生版本

## 作用

- 🔄 同步 xmszm skill 到 `~/.claude/skills/xmszm`
- 💾 自动备份原有的 skill（可选）
- 🛡️ 提供验证机制确保更新成功
- 🔍 支持模拟运行（--dry-run）

## 使用方法

### Linux/Mac/Git Bash

```bash
cd xmszm/scripts
./sync-skill.sh [选项]
```

### Windows

```cmd
cd xmszm\scripts
sync-skill.bat [选项]
```

## 选项说明

| 选项 | 说明 | 备注 |
|------|------|------|
| `-h, --help` | 显示帮助信息 | |
| `-f, --force` | 强制覆盖，不需确认 | 默认会提示确认 |
| `-b, --no-backup` | 不备份原有的 skill | 默认会自动备份 |
| `--dry-run` | 模拟运行 | 仅显示将执行的操作，不实际执行 |

## 使用示例

### 示例 1：正常更新（推荐）

```bash
# 默认行为：有备份，需要确认
./sync-skill.sh
```

输出：
```
===== xmszm Skill 更新工具 =====

源目录: E:\HundredsCompany\rule\xmszm
目标目录: /home/user/.claude/skills/xmszm

✓ 源目录有效
备份原有的 skill...
备份路径: /home/user/.claude/skills/xmszm.backup-20260107-143000
✓ 备份完成

正在更新 skill...
✓ 更新完成

✓ 验证成功: 目标 skill 有效

信息:
  源目录: E:\HundredsCompany\rule\xmszm
  目标目录: /home/user/.claude/skills/xmszm
  备份目录: /home/user/.claude/skills/xmszm.backup-20260107-143000

===== 更新完成 =====
```

### 示例 2：强制覆盖

```bash
./sync-skill.sh -f
```

- 不需要确认
- 仍然会创建备份
- 立即执行更新

### 示例 3：强制覆盖，不备份

```bash
./sync-skill.sh -f --no-backup
```

- 不需要确认
- 不创建备份
- 直接覆盖目标目录

### 示例 4：模拟运行

```bash
./sync-skill.sh --dry-run
```

输出：
```
===== xmszm Skill 更新工具 =====

源目录: E:\HundredsCompany\rule\xmszm
目标目录: /home/user/.claude/skills/xmszm

✓ 源目录有效
[DRY-RUN] 模拟执行以下操作:
rm -rf '/home/user/.claude/skills/xmszm'
cp -r 'E:\HundredsCompany\rule\xmszm' '/home/user/.claude/skills/xmszm'

[DRY-RUN] 模拟执行完成
如果上述操作符合预期，运行不带 --dry-run 参数的命令来实际执行
```

## 工作原理

### 文件检查

1. 检查源目录是否存在
2. 检查源目录是否有效（必须包含 SKILL.md）
3. 检查目标目录是否存在

### 备份流程

如果目标目录已存在且启用备份：
1. 生成备份目录名称：`xmszm.backup-YYYYMMDD-HHMM`
2. 完整复制目标目录到备份位置
3. 显示备份完成信息

### 更新流程

1. 删除旧的目标目录（如存在）
2. 复制源目录到目标位置
3. 验证更新是否成功

### 验证机制

更新完成后验证：
- 检查目标目录是否存在
- 检查 SKILL.md 是否存在
- 确保 skill 有效且完整

## 备份位置

备份文件位于 `~/.claude/skills/` 目录下，命名格式：

```
xmszm.backup-YYYYMMDD-HHMM
```

例如：
```
~/.claude/skills/xmszm.backup-20260107-143000
~/.claude/skills/xmszm.backup-20260108-090530
```

## 故障排除

### 问题 1：找不到脚本

**原因**：脚本路径不正确

**解决**：
```bash
# 正确的路径
cd /path/to/xmszm/scripts
./sync-skill.sh

# 或者从任何目录运行
/path/to/xmszm/scripts/sync-skill.sh
```

### 问题 2：权限被拒绝

**原因**（Linux/Mac）：脚本没有执行权限

**解决**：
```bash
chmod +x sync-skill.sh
./sync-skill.sh
```

### 问题 3：源目录无效

**错误信息**：`错误: 源目录不是有效的 xmszm（缺少 SKILL.md）`

**原因**：源目录中缺少 SKILL.md

**解决**：检查是否在正确的 xmszm 目录中运行

### 问题 4：目标目录权限不足

**原因**：没有 `~/.claude/skills/` 的写入权限

**解决**：检查目录权限
```bash
ls -ld ~/.claude/skills/
chmod 755 ~/.claude/skills/
```

## 恢复备份

如果需要恢复之前的备份：

```bash
# 列出所有备份
ls -la ~/.claude/skills/xmszm.backup-*

# 恢复特定备份（用你的备份名称替换）
rm -rf ~/.claude/skills/xmszm
cp -r ~/.claude/skills/xmszm.backup-20260107-143000 ~/.claude/skills/xmszm
```

## 组合用法

### 场景 1：首次安装

```bash
./sync-skill.sh
```

交互式安装，有备份保护。

### 场景 2：日常更新

```bash
./sync-skill.sh -f
```

快速更新，自动备份，无需确认。

### 场景 3：检查更新

```bash
./sync-skill.sh --dry-run
```

查看将执行的操作，不实际修改任何内容。

### 场景 4：强制重装

```bash
./sync-skill.sh -f --no-backup
```

完全覆盖，不保留旧版本。

## 相关文档

- [SKILL.md](../SKILL.md) - xmszm Skill 入口
- [CODE-WORKFLOW.md](../guides/CODE-WORKFLOW.md) - 代码开发工作流
- [LOG-SKILL.md](../guides/LOG-SKILL.md) - 日志管理工作流

## 常见问题

**Q: 更新后 Claude Code 识别不到新的 skill？**

A: Claude Code 通常会自动刷新 skills 列表。如果没有识别到，尝试：
1. 重启 Claude Code
2. 重新加载窗口（Cmd/Ctrl + R）
3. 检查 `~/.claude/skills/xmszm/SKILL.md` 是否存在

**Q: 可以保留多个备份吗？**

A: 可以的。备份文件不会被自动删除，可以手动管理。

**Q: 脚本支持哪些系统？**

A:
- `sync-skill.sh`：Linux、Mac、Windows (Git Bash)
- `sync-skill.bat`：Windows

**Q: 可以在其他目录运行脚本吗？**

A: 可以。脚本会自动根据脚本位置来确定源目录位置。

```bash
/path/to/xmszm/scripts/sync-skill.sh
```
